spring:
  ai:
    openai:
      api-key: test-key-for-testing-only
  r2dbc:
    url: r2dbc:h2:mem:///testdb
  data:
    redis:
      host: localhost
      port: 6379
  security:
    oauth2:
      client:
        registration:
          test:
            client-id: test-client
            client-secret: test-secret
